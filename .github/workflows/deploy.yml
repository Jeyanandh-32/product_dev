name: Deploy Sparrow POS to AWS EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect Changed Components
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      merchant: ${{ steps.filter.outputs.merchant }}
      customer: ${{ steps.filter.outputs.customer }}
      terminal: ${{ steps.filter.outputs.terminal }}
      landing: ${{ steps.filter.outputs.landing }}
      migrations: ${{ steps.filter.outputs.migrations }}
      workflow: ${{ steps.filter.outputs.workflow }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Filter Changed Paths
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'packages/**'
            merchant:
              - 'merchant/**'
              - 'packages/**'
            customer:
              - 'customer/**'
              - 'packages/**'
            terminal:
              - 'terminal/**'
              - 'packages/**'
            landing:
              - 'landing/**'
            migrations:
              - 'backend/migrations/**'
            workflow:
              - '.github/workflows/deploy.yml'

  build:
    name: Build & Package Changes
    needs: changes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Flutter & Dart SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install Workspace Dependencies
        run: |
          dart pub global activate melos
          export PATH="$PATH":"$HOME/.pub-cache/bin"
          melos bootstrap

      - name: Run Tests on Changed Packages
        run: |
          export PATH="$PATH":"$HOME/.pub-cache/bin"
          dart analyze
          if [ "${{ needs.changes.outputs.backend }}" = "true" ] || [ "${{ needs.changes.outputs.workflow }}" = "true" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            cd backend && dart test && cd ..
          fi
          if [ "${{ needs.changes.outputs.customer }}" = "true" ] || [ "${{ needs.changes.outputs.workflow }}" = "true" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            cd customer && dart test && cd ..
          fi
          if [ "${{ needs.changes.outputs.terminal }}" = "true" ] || [ "${{ needs.changes.outputs.workflow }}" = "true" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            cd terminal && flutter test && cd ..
          fi

      - name: Build Merchant Web App (WASM)
        if: needs.changes.outputs.merchant == 'true' || needs.changes.outputs.workflow == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          cd merchant
          dart pub global activate jaspr_cli
          export PATH="$PATH":"$HOME/.pub-cache/bin"
          jaspr build --experimental-wasm --dart-define=API_URL=https://pos.sparrow-x.in

      - name: Build Customer Store Web App (WASM)
        if: needs.changes.outputs.customer == 'true' || needs.changes.outputs.workflow == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          cd customer
          dart pub global activate jaspr_cli
          export PATH="$PATH":"$HOME/.pub-cache/bin"
          jaspr build --experimental-wasm --dart-define=API_URL=https://pos.sparrow-x.in

      - name: Build & Compile Native Backend Binary
        if: needs.changes.outputs.backend == 'true' || needs.changes.outputs.workflow == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          export PATH="$PATH":"$HOME/.pub-cache/bin"
          dart pub global activate dart_frog_cli
          cd backend
          dart pub global run dart_frog_cli:dart_frog build
          mv build /tmp/backend-build
          cd /tmp/backend-build
          dart pub get
          dart compile exe bin/server.dart -o $GITHUB_WORKSPACE/server.exe

      - name: Package Changed Artifacts
        run: |
          mkdir -p dist
          if [ -d "merchant/build/jaspr" ]; then
            mkdir -p dist/merchant
            cp -r merchant/build/jaspr/* dist/merchant/
          fi
          if [ -d "customer/build/jaspr" ]; then
            mkdir -p dist/store
            cp -r customer/build/jaspr/* dist/store/
          fi
          if [ "${{ needs.changes.outputs.landing }}" = "true" ] || [ "${{ needs.changes.outputs.workflow }}" = "true" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            mkdir -p dist/landing
            cp -r landing/* dist/landing/
          fi
          if [ "${{ needs.changes.outputs.migrations }}" = "true" ] || [ "${{ needs.changes.outputs.workflow }}" = "true" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            mkdir -p dist/migrations
            cp -r backend/migrations/* dist/migrations/
          fi
          if [ -f "server.exe" ]; then
            cp server.exe dist/
          fi

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-dist
          path: dist/

  deploy:
    name: Deploy to EC2
    needs: [changes, build]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-dist
          path: dist/

      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy Selective Artifacts via Rsync
        run: |
          if [ -d "dist/landing" ]; then
            rsync -avz -e "ssh -i ~/.ssh/id_rsa" dist/landing/ ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/var/www/landing/
          fi
          if [ -d "dist/merchant" ]; then
            rsync -avz -e "ssh -i ~/.ssh/id_rsa" dist/merchant/ ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/var/www/merchant/
          fi
          if [ -d "dist/store" ]; then
            rsync -avz -e "ssh -i ~/.ssh/id_rsa" dist/store/ ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/var/www/store/
          fi
          if [ -d "dist/migrations" ]; then
            rsync -avz -e "ssh -i ~/.ssh/id_rsa" dist/migrations/ ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/home/${{ secrets.EC2_USER }}/pos/migrations/
          fi
          if [ -f "dist/server.exe" ]; then
            rsync -avz -e "ssh -i ~/.ssh/id_rsa" dist/server.exe ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:/home/${{ secrets.EC2_USER }}/pos/server.exe
          fi

      - name: Restart Backend Service
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
            if [ -f "/home/${{ secrets.EC2_USER }}/pos/server.exe" ]; then
              chmod +x /home/${{ secrets.EC2_USER }}/pos/server.exe
              sudo systemctl restart pos-backend
            fi
          EOF

