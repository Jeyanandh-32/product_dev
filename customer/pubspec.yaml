name: customer
description: Customer web application for online ordering.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  api_client:
    path: ../packages/api_client
  change_case: ^2.2.0
  client_repositories:
    path: ../packages/client_repositories
  dio: ^5.9.2
  jaspr: ^0.23.2
  jaspr_lucide: ^0.55.0
  jaspr_router: ^0.9.0
  models:
    path: ../packages/models
  signals: ^7.1.0
  validators:
    path: ../packages/validators
  web: ^1.1.1

dev_dependencies:
  build_daemon: ^4.1.3
  build_runner: ^2.15.1
  build_web_compilers: ^4.8.5
  jaspr_builder: ^0.23.2
  lints: ^6.0.0
  test: ^1.25.6

jaspr:
  mode: client
